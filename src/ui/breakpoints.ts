export const breakpointSize = {
  mobile: 430,
  tablet: 768,
  laptop: 1024,
  desktop: 1920,
};

export const device = {
  mobile: `(max-width: ${breakpointSize.mobile}px)`,
  tablet: `(max-width: ${breakpointSize.tablet}px)`,
  laptop: `(max-width: ${breakpointSize.laptop}px)`,
  desktop: `(max-width: ${breakpointSize.desktop}px)`,
};
